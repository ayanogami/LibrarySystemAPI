services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=library_system_test'
      - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD}'
      - 'POSTGRES_USER=${POSTGRES_USER}'
    ports:
      - '5432:5432'
